[project]
name = "gestor-tareas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "base64-python>=0.6",
    "flask>=3.1.3",
    "pymongo[aws,encryption,gssapi,ocsp,snappy,zstd]>=4.16.0",
]
